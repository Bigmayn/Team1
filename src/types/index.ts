export interface NavItem {
  title: string;
  href: string;
}

export interface HeroProps {
  title: string;
  description: string;
  imageUrl: string;
}

export interface ProductCardProps {
  title: string;
  description: string;
  imageUrl: string;
}